# Copy this file to .streamlit/secrets.toml and fill in your key for local runs.
# Never commit the real secrets.toml — it's gitignored.
#
# On Streamlit Community Cloud: App settings -> Secrets -> paste the same line.

ANTHROPIC_API_KEY = ""
